---
  - name: Creates route53 entry
    hosts: localhosts
    tasks: 
      - name: Route53 a record
        route53:
          state: "{{state}}"
          zone: "{{zone}}"
          record: "{{record}}"."{{zone}}"
          type: A
          ttl: 7200 
          value: "{{IP}}"
          