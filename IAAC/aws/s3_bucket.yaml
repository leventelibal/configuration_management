---
  - name: creates and deletes S3 buckets
    hosts: all
    tasks: 
      - name: S3 Management
        aws_s3:
          bucketname: "{{bucket}}"
          mode: "{{mode}}"
        #  region: "{{region}}" #never delete bucket just empty bucket  