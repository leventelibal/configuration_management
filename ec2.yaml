--- #sudo pip install boto
- name: Creates Ec2 instance
  host: localhost
  tasks:
    -name: Create instance 
    ec2:
      key_name: mywindows
      instance_type: t2.micro
      image: ami-0756fbca465a59a30
      wait: yes
      count: 3
      vpc_subnet_id: subnet-dac889bd
      region: us-east-1